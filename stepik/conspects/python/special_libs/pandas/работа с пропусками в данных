 ОТСТУТСТВУЮЩИЕ ДАННЫЕ:

- отсутствующие данные возникают при различных размерах столбцов/строк, например:
    * df = pd.read_csv(StringIO('price,count,percent\n1,10,\n2,20,51\n3,30,'))  - здесь будет 2 элемента NaN(в последнем столбце)
    * pd.isnull(df)                  - таблица [True/False], где True <=> элемент == NaN
    * df.fillna(0)                   - таблица, в которой NaN заменен на 0
    * df.fillna(df.mean())           - замена NaN на стреднее значение по столбцу
    * df.dropna()                    - удаление всех строк содержащих NaN
    * df.dropna(axis=1)              - удаление всех столбцов, содержащих NaN
    * df.dropna(axis = 1, thresh=3)  - удаление всех столбцов содержащих меньше 3 не-NaN элементов
