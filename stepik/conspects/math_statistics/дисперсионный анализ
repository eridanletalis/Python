 ДИСПЕРСИОННЫЙ АНАЛИЗ:

- ПОДГОТОВКА:
    * пусть у нас есть 3 выборки со средними m1, m2, m3
    * тогда выдвинем 2 гипотезы:
        * H0: M1 == M2 == M3
        * H1: M1 != M2 != M3 (хотя бы одна пара не равна)
    * вычислим среднее значение всех наблюдений:
        * X = x1 + x2 + x3                          - рассматриваем все выборки как одну
        * m0 = sum(Xi) / len(X)                     - сумма всех значений выборок на кол-во значений
    * вычислим общую сумму квадратов:
        * sst = sum((Xi - m0)^2) for i in X    - xI - элемент каждой выборки
        * df = len(X) - 1                      - число степеней свободы вычисляется как и раньше
    * определим общую изменчивость:
        * общая изменчивость зависит:
            * ssw - показатель изменчивости внутри конкретной группы
                * ssw = sum((x1i - m1)^2) + sum((x2i - m2)^2) + sum((x3i - m3)^2)  - сумма квадратов разности значений и среднего в группах
                * df = len(X) - group_count
            * ssb - показатель изменчивости между соотв.элементами разных групп:
                * ssb = sum(len(xi) * (ssw(xi) - m0))
                * df = group_count - 1
        * рассчитаем основной статистический показатель дисперсионного анализа(f-значение):
            * f = (ssb/df(ssb)) / (ssw/df(ssw))
        * рассчитаем p-значение используя f, df(ssb), df(ssw)
        * отклоняем или оставляем H0, в зависимости от значения p

