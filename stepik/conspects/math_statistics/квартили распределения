 КВАРТИЛИ РАСПРЕДЕЛЕНИЯ:

- три точки(значения признака), которые делят УПОРЯДОЧЕННОЕ мн-во данных на 4 равные части
- вычисление квартилей:
    * найти медиану
    * для половины слева и справа от медианы так же найти медиану
    * полученные 3 значения будут квартилями
- построение BoxPlot(ящик с усами):
    * сам ящик - значения в интервале [q1, q3]
    * усы - значения в интервалах [q1 - 1.5*len([q1, q3]), q1] и [q3, q3 + 1.5*len([q1, q3])]
    * все значения не попавшие в усы отображаются отдельными точками
- информация в BoxPlot:
    * лучше всего сравнивать с другим BoxPlot
    * удобно сравнивать медианы
    * удобно смотреть разброс элементов в контейнерах
    * сразу видно min и max значения, а так же выбросы