 ТИПЫ ПЕРЕМЕННЫХ:

- переменные - характеристики, которые мы будем рассматривать как переменные

КОЛИЧЕСТВЕННЫЕ(измеренные значения некоторой характеристики) :
    - непрерывные:
        * принимает любое значение на некотором промежутке
        * ПРИМЕР: рост [140, 250] (может быть равным 157.23)
    - дискретные:
        * принимает только определенное значение
        * ПРИМЕР: кол-во детей в семье {0, 1 ... 25} (3.5 ребенка в семье не допускается)

НОМИНАТИВНЫЕ(значения для разделения испытуемых на группы):
    - эти значения не несут в себе мат.смысла, даже если их значения цифры
    - этими значениями нельзя никак оперировать, кроме как getValue
    - по сути это словарь (ключ-значение)
    - ПРИМЕР: '1' - M, '2' - Ж

РАНГОВЫЕ(значения только для сравнения):
    - эти значения можно ТОЛЬКО сравнивать
    - ПРИМЕР: [1....n], в котором рост n-ого участника: n > n - 1


