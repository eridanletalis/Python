 Сортировка слиянием:

ВХОД: массив A[1...n]
ВЫХОД: отсортированный массив A[1...n]
ИДЕЯ:
- разбить массив пополам, отсортировать половины и слить в один
- разбивать куски, пока обе половины не окажутся отсортированными
РЕАЛИЗАЦИЯ:
    def merge_sort(A, a, b):
        if a < b:
            m = a + (b - a)//2
            merge(merge_sort(A, a, m), merge_sort(A, m + 1, b))  - сливаем 2 отсортированных массива в 1
СЛОЖНОСТЬ:
- O(n*log(n))